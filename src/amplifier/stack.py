"""Amplifier stack with overflow protection."""

from collections import deque
from typing import Any, Deque

class AmplifierStack:
    """Stack limited to 5 items to prevent overflow."""

    MAX_SIZE = 5

    def __init__(self) -> None:
        self._stack: Deque[Any] = dequePerfekt — bauen wir den Motor. Hier ist das komplette `src/`-Paket, das dein Hub Pilot sucht. Einfach die Ordner/Dateien anlegen, reinkopieren, pushen — danach ist „keine KI eingetragen“ weg.

### 1️⃣ `src/__init__.py`
```python
"""Multi-Tag Agent Framework – production package."""
__version__ = "0.1.0"